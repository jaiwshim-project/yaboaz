(function(){
  'use strict';
  if(document.body.dataset.page!=='ontology')return;
  var KEY='k-fde-ontology-v1',BASE='k-fde-mvp-v2';
  var elements=[
    ['object','01','객체','무엇을 계속 추적하고 식별해야 하는가?','사람·설비·업무·문서처럼 정체성, 상태, 관계 또는 생명주기를 가진 핵심 대상을 고릅니다.','객체 목록·고유 식별자·제외 근거'],
    ['attribute','02','속성','객체를 어떻게 설명하고 비교하는가?','단위·허용값·필수 여부·결측 처리까지 정해 판단에 필요한 최소 속성을 확정합니다.','속성 사전·품질 규칙·샘플 값'],
    ['relation','03','관계','객체는 어떻게 연결되고 영향을 주는가?','관계의 방향·유형·카디널리티·유효기간을 정해 영향과 책임의 경로를 만듭니다.','관계 유형·from/to·영향 경로'],
    ['state','04','상태','현재 조건을 어떤 상태로 표현하는가?','정상·주의·위험·종료 상태와 상태를 바꾸는 조건을 함께 정의합니다.','상태 목록·전이 조건·현재값'],
    ['event','05','이벤트','무슨 일이 상태를 바꾸는가?','발생 시각·위치·주체·원본을 가진 사건을 트리거로 기록합니다.','이벤트 유형·트리거·페이로드'],
    ['rule','06','규칙','어떤 조건에서 어떻게 판단하는가?','IF-THEN 조건, 우선순위, 예외, 충돌 처리와 규칙 오너를 명시합니다.','판정 규칙·우선순위·예외 사례'],
    ['action','07','행동','판단 뒤 무엇을 실행하는가?','사람·시스템·외부 기관의 실행, 승인·취소·롤백 조건을 안전하게 연결합니다.','행동 카탈로그·승인자·롤백 절차']
  ];
  var relationTypes=['FLOW','INPUT','OUTPUT','USES','CREATES','DECIDES','TRIGGERS','CAUSES','BLOCKS'];
  function esc(v){return String(v==null?'':v).replace(/[&<>'"]/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c];});}
  function parse(k,f){try{return JSON.parse(localStorage.getItem(k)||'')||f;}catch(_){return f;}}
  function projectId(){return parse(BASE,{}).projectId||'firenavi';}
  function get(){var all=parse(KEY,{projects:{}});all.projects=all.projects||{};var id=projectId();if(!all.projects[id])all.projects[id]={done:[],relations:[],evidence:'',checks:[]};return{all:all,id:id,data:all.projects[id]};}
  var state=get();
  function save(){state.all.projects[state.id]=state.data;localStorage.setItem(KEY,JSON.stringify(state.all));}
  function selectOptions(kind,current){return elements.map(function(e){return'<option value="'+e[0]+'"'+(e[0]===current?' selected':'')+'>'+e[2]+'</option>';}).join('');}
  function relationOptions(){return relationTypes.map(function(r){return'<option>'+r+'</option>';}).join('');}
  function render(){var page=document.querySelector('.studio-page');if(!page||page.querySelector('.og-wrap'))return;var done=state.data.done||[],relations=state.data.relations||[],checks=state.data.checks||[],percent=Math.round(done.length/elements.length*100);var cards=elements.map(function(e){var on=done.indexOf(e[0])>-1;return'<article class="og-element'+(on?' done':'')+'"><span class="og-symbol">'+e[1]+'</span><div><h4>'+e[2]+'</h4><p><strong>발견 질문</strong> '+e[3]+'<br>'+e[4]+'</p><label><input type="checkbox" data-og-element="'+e[0]+'"'+(on?' checked':'')+'> 구조화 완료 · '+e[5]+'</label></div></article>';}).join('');var rels=relations.length?'<ul>'+relations.map(function(r,i){return'<li><span>'+esc(label(r.from))+' <b>'+esc(r.type)+'</b> '+esc(label(r.to))+'</span><button data-og-remove="'+i+'" aria-label="연결 제거">×</button></li>';}).join('')+'</ul>':'<p class="og-tip">아직 연결이 없습니다. 핵심 객체부터 관계를 하나씩 추가하세요.</p>';var checkLabels=['모든 요소에 이름·정의·소유자가 있다','관계의 from/to 노드가 모두 존재한다','상태를 바꾸는 이벤트와 규칙이 연결되어 있다','행동에는 승인자와 롤백 조건이 있다','Issue가 있으면 CAUSES 또는 BLOCKS 관계가 있다'];var checklist=checkLabels.map(function(c,i){return'<label class="og-check"><input type="checkbox" data-og-check="'+i+'"'+(checks.indexOf(i)>-1?' checked':'')+'>'+c+'</label>';}).join('');page.insertAdjacentHTML('beforeend','<section class="og-wrap"><section class="og-intro"><div><small>ONTOLOGY 7-ELEMENT OPERATING GUIDE</small><h3>현장의 명사를 AI가 이해하는 실행지식으로 바꾸는 법</h3><p>자료를 읽고 바로 노드를 만드는 것이 아니라, 발견 질문으로 대상을 확인하고 관계를 연결한 뒤 상태·이벤트·규칙·행동의 실행 흐름을 검증합니다.</p></div><aside><strong>권장 순서</strong><span>객체 → 속성 → 관계 → 상태 → 이벤트 → 규칙 → 행동</span><span>완료율 '+percent+'% · '+done.length+'/7 요소 구조화</span></aside></section><section class="og-flow"><article><b>1</b><strong>발견</strong><span>현장 용어·사례·증거에서 핵심 대상을 찾습니다.</span></article><article><b>2</b><strong>연결</strong><span>객체 사이 관계와 영향 경로를 그립니다.</span></article><article><b>3</b><strong>구조화</strong><span>상태·이벤트·규칙·행동으로 실행 가능하게 만듭니다.</span></article><article><b>4</b><strong>검증</strong><span>중복·고립·충돌·근거 누락을 체크합니다.</span></article></section><div class="og-grid"><section class="og-panel"><header class="og-head"><div><h3>7요소 발견 카드</h3><p>인터뷰·증거 검토 후 각 카드의 완료를 체크하세요.</p></div><span class="og-progress">'+percent+'%</span></header><div class="og-elements">'+cards+'</div></section><aside class="og-side"><section class="og-card"><h3>관계 연결 워크벤치</h3><p>두 요소를 선택하고 관계를 지정하면 프로젝트 온톨로지에 저장됩니다.</p><div class="og-relation"><select data-og-rel="from">'+selectOptions('from','object')+'</select><span>→</span><select data-og-rel="type">'+relationOptions()+'</select><select data-og-rel="to">'+selectOptions('to','event')+'</select></div><button class="og-save" data-og-action="add-relation">관계 저장</button><div class="og-relations">'+rels+'</div></section><section class="og-card"><h3>구조화 검증 체크리스트</h3><p>AI 판단이나 플랫폼 구현으로 넘기기 전 아래 조건을 확인합니다.</p><div class="og-checks">'+checklist+'</div></section><section class="og-card"><h3>증거·출처 메모</h3><p>각 요소를 발견한 인터뷰 발언, 문서, 로그의 위치를 남깁니다.</p><div class="og-evidence"><textarea data-og-evidence placeholder="예: 안전관리자 인터뷰 2026-08-15, 점검표 3행">'+esc(state.data.evidence||'')+'</textarea><button data-og-action="save-evidence">출처 메모 저장</button></div></section></aside></div><div class="og-tip"><strong>FDE 실무 팁</strong> · 객체를 먼저 확정하고, 관계를 연결한 다음 상태·이벤트·규칙·행동을 시간 순서로 검증하세요. 이름만 있는 노드는 구조화가 끝난 것이 아니며, 반드시 출처와 실행 조건이 있어야 합니다.</div></section>');bind();}
  function label(id){var e=elements.find(function(x){return x[0]===id;});return e?e[2]:id;}
  function bind(){document.querySelectorAll('[data-og-element]').forEach(function(el){el.addEventListener('change',function(){var i=state.data.done.indexOf(el.dataset.ogElement);if(el.checked&&i<0)state.data.done.push(el.dataset.ogElement);if(!el.checked&&i>-1)state.data.done.splice(i,1);save();renderAgain();});});document.querySelectorAll('[data-og-check]').forEach(function(el){el.addEventListener('change',function(){var i=Number(el.dataset.ogCheck),a=state.data.checks||[],p=a.indexOf(i);if(el.checked&&p<0)a.push(i);if(!el.checked&&p>-1)a.splice(p,1);state.data.checks=a;save();});});var evidence=document.querySelector('[data-og-evidence]');if(evidence)evidence.addEventListener('input',function(){state.data.evidence=evidence.value;save();});}
  function renderAgain(){var page=document.querySelector('.studio-page'),wrap=page&&page.querySelector('.og-wrap');if(wrap)wrap.remove();render();}
  document.addEventListener('click',function(e){var b=e.target.closest('[data-og-action]');if(b&&b.dataset.ogAction==='add-relation'){var box=document.querySelector('.og-relation'),from=box.querySelector('[data-og-rel="from"]').value,type=box.querySelector('[data-og-rel="type"]').value,to=box.querySelector('[data-og-rel="to"]').value;if(from===to){alert('같은 요소끼리는 관계를 만들 수 없습니다.');return;}state.data.relations=state.data.relations||[];if(!state.data.relations.some(function(r){return r.from===from&&r.type===type&&r.to===to;}))state.data.relations.push({from:from,type:type,to:to});save();renderAgain();}else if(b&&b.dataset.ogAction==='save-evidence'){save();b.textContent='저장됨 ✓';setTimeout(function(){b.textContent='출처 메모 저장';},1600);}var remove=e.target.closest('[data-og-remove]');if(remove){state.data.relations.splice(Number(remove.dataset.ogRemove),1);save();renderAgain();}});
  var observer=new MutationObserver(function(){if(document.querySelector('.studio-page')&&!document.querySelector('.og-wrap'))render();});observer.observe(document.getElementById('app'),{childList:true,subtree:true});
  render();
})();
