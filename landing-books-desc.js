(function(){
  function apply(){
    var p=document.querySelector('.book-library header p');
    if(p)p.textContent='현장 실행·2A4 문제해결·온톨로지·AI·미래·바이브 코딩·창업·리더십·조직 협업·설득·소통까지, YABOAZ를 구성하는 핵심 주제를 실제 책과 함께 확인합니다.';
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){setTimeout(apply,40);});else apply();
})();
